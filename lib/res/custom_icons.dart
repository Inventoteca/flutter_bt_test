import 'package:flutter/material.dart';

class CustomIcons {
  static const uvIcon = Icon(
    Icons.sunny,
    color: Colors.white,
    size: 60,
  );
  final Icon tempIcon = const Icon(Icons.thermostat_auto_rounded);
  final Icon humIcon = const Icon(Icons.water_drop);
}
